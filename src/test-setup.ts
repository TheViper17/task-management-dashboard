/**
 * Global test setup — runs before every spec file.
 *
 * The Angular TestBed and application polyfills are initialised by the
 * `@angular/build:unit-test` builder *before* this file. Here we only add
 * cross-cutting test utilities.
 */
import '@testing-library/jest-dom/vitest';
